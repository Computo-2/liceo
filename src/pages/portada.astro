<!-- Componente sin lógica -->
<section class="relative flex items-center justify-center min-h-[35svh] md:min-h-[72svh] lg:min-h-[87vh] overflow-hidden font-sans text-2xl">
  <!-- Contenedor del video de fondo -->
  <div class="absolute inset-0 overflow-hidden -z-10 bg-black">
    <iframe
      class="vf-iframe"
      src="https://player.vimeo.com/video/1116858454?background=1&autoplay=1&muted=1&loop=1&playsinline=1"
      allow="autoplay; fullscreen; picture-in-picture"
      allowfullscreen
      title="Background video"
    ></iframe>

    <!-- Degradado inferior -->
    <div class="pointer-events-none absolute bottom-0 left-0 w-full h-[120px] md:h-[160px] lg:h-[200px] bg-gradient-to-t from-[rgba(192,190,190,0.8)] to-transparent z-10"></div>
  </div>

  <!-- Imagen flotante -->
  <div class="absolute bottom-[12px] right-[12px] z-10 md:bottom-[24px] md:right-[24px]">
    <img
      src="/CONTACTANOS.svg"
      alt="Liceo Michoacano"
      class="w-[130px] md:w-[260px] lg:w-[300px] max-w-full cursor-pointer transition-transform duration-200 ease-in-out hover:scale-105 active:scale-95"
    />
  </div>
</section>

<style is:global>
  /* En <head>:
     <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"> */

  .vf-iframe {
    position: absolute;
    left: 50%;
    top: 50%;
    border: 0;
    transform: translate(-50%, -50%);
    pointer-events: none;
    background: transparent;
  }

  /* ===== Móvil: CONTAIN (video completo, aunque con franjas) ===== */
  @media (max-width: 768px) and (min-aspect-ratio: 16/9) {
    .vf-iframe {
      width: calc(100dvh * 16 / 9);
      height: 100dvh;
    }
  }
  @media (max-width: 768px) and (max-aspect-ratio: 16/9) {
    .vf-iframe {
      width: 100dvw;
      height: calc(100dvw * 9 / 16);
    }
  }

  /* ===== iPad y Desktop: COVER (sin franjas, puede recortar) ===== */
  @media (min-width: 769px) and (min-aspect-ratio: 16/9) {
    .vf-iframe {
      width: 110dvw;   /* overscan para matar bordes blancos */
      height: 61.9dvw;
    }
  }
  @media (min-width: 769px) and (max-aspect-ratio: 16/9) {
    .vf-iframe {
      width: 196dvh;
      height: 110dvh;
    }
  }

  /* Ajuste de encuadre */
  @media (max-width: 768px) { .vf-iframe { top: 65%; } }
  @media (min-width: 769px) { .vf-iframe { top: 50%; } }

  @supports (-webkit-touch-callout: none) {
    .vf-iframe { will-change: transform; }
  }
</style>
